name: deploy-backend

on:
  push:
    branches: [ main ]
    
jobs:
  build:
    runs-on: self-hosted
    
    steps:
    - uses: actions/checkout@v3
      with:
        clean: false
    
    # Down Docker-compose container
    - name: Docker Stopping
      run: docker compose down
      
    # Delete Docker Images (Frontend, Backend)
    - name: Remove Docker frontend and backend images
      run: docker rmi cp23ms2-backapp

    # Fetch latest version from branch dev 
    - name: Fetching Latest Data
      run: git fetch
      
    # Pull from Github 
    - name: Pulling Latest Data
      run: git pull
          
    # Start Docker-compose
    - name: Starting Docker Container
      run: docker compose up -d
